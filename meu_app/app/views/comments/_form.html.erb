<%= form_with(model: comment, html: { class: "space-y-6 max-w-md mx-auto bg-white p-6 rounded shadow" }) do |form| %>
  <% if comment.errors.any? %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
      <h2 class="font-bold mb-2"><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul class="list-disc pl-5">
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, "Conteúdo", class: "block text-gray-700 font-semibold mb-2" %>
    <%= form.text_area :content, class: "w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <div>
    <%= form.label :post_id, "Post ID", class: "block text-gray-700 font-semibold mb-2" %>
    <%= form.text_field :post_id, class: "w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <div class="text-center">
    <%= form.submit class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition cursor-pointer" %>
  </div>
<% end %>
