<div>

    <% if admin_signed_in? %>
        <p>Olá, <%= current_admin.email %></p>

        <%= link_to "Deslogar", destroy_admin_session_path, method: :delete, data: { turbo_method: :delete } %>
    <% end%>
    <ul>
        <li>
            <%= link_to "Home", root_path %>
        </li>

        <li>
            <%= link_to "Post", posts_path %>
        </li>

        <li>
            <%= link_to "Comment", comments_path %>
        </li>
    </ul>
</div>
